version: 0.2.{build}.0

configuration: Release

platform: Any CPU

assembly_info:
  patch: true
  file: GraveRobber\Properties\AssemblyInfo.cs
  assembly_version: '0.2.0.0'
  assembly_file_version: '{version}'
  assembly_informational_version: '0.2.0'

before_build:
- nuget restore

build:
  project: GraveRobber.sln
  parallel: true
  verbosity: minimal
